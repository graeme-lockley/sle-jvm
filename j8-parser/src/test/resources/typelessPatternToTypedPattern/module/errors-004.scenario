-- Duplicate let declarations error where both have a type signature
add : Int -> Int -> Int
add =
    \a b -> a + b

add : Int -> Int -> Int
add =
    \x y -> x + y
-- errors
[
    DuplicateLetSignature(location=[(5, 0) (5, 22)], otherLocation=[(1, 0) (1, 22)], name=add)
]