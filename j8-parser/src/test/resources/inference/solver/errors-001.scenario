-- Error if attempt to reference an unknown typealias
typealias BinaryIntMap =
    SomeInt -> Int

let factorial n : BinaryIntMap =
    if n == 0 then
        1
    else
        n * factorial (n - 1)
-- errors
[
  UnknownType(name=SomeInt)
]
