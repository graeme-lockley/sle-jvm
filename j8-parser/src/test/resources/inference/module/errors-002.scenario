-- Duplicate let declarations error where first has a type signature
add : Int -> Int -> Int =
    \a b -> a + b

add x y =
    x + y
-- errors
[
    DuplicateLetDeclaration(location=[(4, 0) (5, 8)], name=add)
]